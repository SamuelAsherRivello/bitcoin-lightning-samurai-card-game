[package]
name = "bevy_card_game_shared"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = ["bevy_log", "bevy_text", "bevy_ui"] }
